json.extract! teacher, :id, :name, :ic, :gender, :nationality, :experiences, :contact, :address, :email, :password, :classroom_id, :created_at, :updated_at
json.url teacher_url(teacher, format: :json)
